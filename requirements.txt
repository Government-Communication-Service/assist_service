# this is needed to install torch cpu flavor so that huge nvidia gpu libs are not installed
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.12.0

# API / General
starlette==0.49.1
fastapi==0.136.3
uvicorn[standard]==0.49.0
gunicorn~=26.0
pydantic>=2.11.0
pydantic-settings[aws-secrets-manager]>=2.11.0
uuid~=1.30
python-multipart~=0.0.9
aiohttp==3.14.1
aiofiles~=24.1
httpx~=0.28.1

# Logging
bugsnag~=4.7.1
boto3>=1.35.0 ### Used for CloudWatch logging and pydantic-settings AWS Secrets Manager
python-json-logger~=3.2.1

# Database
alembic==1.18.4
psycopg2-binary==2.9.12
asyncpg==0.31.0
SQLAlchemy[asyncpg]==2.0.50


# Document parsing
pdfminer.six>=20251230
unstructured[pdf,docx,pptx,xlsx]==0.22.32
# spaCy model required by unstructured for document partitioning.
# unstructured pulls in spaCy but not the model, so pin it here to install at
# build time instead of lazily downloading at runtime (fails on read-only
# site-packages, e.g. the non-root user on Elastic Beanstalk).
# Version must track the installed spaCy minor line (currently 3.8.x ->
# en_core_web_sm 3.8.x). After a spaCy bump, run `python -m spacy validate`
# and update this URL to the version it reports as compatible.
en_core_web_sm @ https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.8.0/en_core_web_sm-3.8.0-py3-none-any.whl
#needed for odt file format processing.
pypandoc~=1.13
# File type detection
python-magic~=0.4.27

# American/British English detection
breame~=0.1.2
uwotm8~=0.1.6

# LLM
anthropic~=0.103


# RAG
opensearch-py~=2.7

# HTML document processing
beautifulsoup4~=4.14.3

# Testing
requests~=2.34
pytest~=8.1.1
pytest-asyncio~=0.23
pytest-mock~=3.14
pytest-xdist~=3.6
moto[s3]==5.2.2

# Development
ruff~=0.6
